[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nightshift"
version = "0.1.0"
description = "Night Shift Python orchestrator skeleton"
requires-python = ">=3.11"

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
